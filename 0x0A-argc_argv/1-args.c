#include <stdio.h>

/**
 * main - prints out the number of arguments passed to the command line
 * @argc: argument count
 * Return: 0 (success)
 */

int main(int argc)
{
	printf("%d\n", argc);

	return (0);
}
