#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: 0 (success)
 * Description: All possible combination fo single digit numbers
 */
int main(void)
{
	int i;

	for (i = '0'; n < '9'; n++)
	{
		putchar(n);
		putchar(',');
		putchar(' ');

		if (n == '9')
			break
	}
	putchar('\n');
	return (0);
}

